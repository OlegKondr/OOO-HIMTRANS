Как собрать и запустить
Создаем папку проекта.

Помещаем  файлы worker.h, (реализация методов), (основная программа) в эту папку.

Компилируем программу (через g++):

bash
g++ (реализация методов) (основная программа) -o worker_program
Запустите исполняемый файл:

bash
./worker_program
